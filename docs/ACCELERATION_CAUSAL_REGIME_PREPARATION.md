# Regime-first research preparation

The existing atr_regime owner gains causal_hysteresis_regime for precomputed causal ADX or relative-volatility observations. This is a research feature; it is not a new indicator or strategy/runtime regime engine. Caller-pinned closed-feature available_at must be strictly before decision_at. The equal-time/future feature value is never consumed. An empty causal prefix yields INSUFFICIENT_SAMPLE/unknown, not invented neutrality. Explicit enter > exit thresholds and consecutive confirmations reduce flapping and expose delay. UTC availability, thresholds, source pin and eligible values are hash-bound; invalid/duplicate/unordered available features fail closed.

H04 hysteresis mechanism is implemented/testable; economic/OOS hypothesis status remains PREPARE. H01/H02 may reuse this helper only after explicit ATR/relative-volatility source preparation. Research order stays REGIME → STRUCTURE → ENTRY. No automatic CAND-001 mutation. Existing immutable trial declarations, multiple-testing preflight and costed OOS/WF validation remain mandatory.

Synthetic tests prove hand-calculated transition/dead-band behavior, causal-prefix invariance, unknown sample, finite/type/time guards and spec identity. They do not establish profitable filters, forward degradation or selected thresholds. No pinned current Candidate gross/cost/trade ledger has been available: numerical efficacy remains WAITING_EXTERNAL.

The single Windows export runner now deploys via clean ancestor-gated detached checkout, with no merge/reset/force operation. Added mandatory PowerShell parser validation is syntax evidence, not Windows-host identity or IG account evidence.
